snapshots:
  - name: scd_silver_airports
    relation: ref('airstats', 'silver_airports')
    config:
      schema: snapshots
      unique_key: airport_ident
      strategy: check
      check_cols: all